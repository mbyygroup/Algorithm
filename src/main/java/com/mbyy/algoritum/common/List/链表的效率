- 从表头插入和删除速度很快，仅需要改变一两个引用值，所以需要花费O(1)时间
- 平均下来，查找，删除和在指定的链节点后面插入都需要搜索链表中的一半链结点，需要O(N)次比较，在数
组执行这些操作也需要O(N)次比较，但链表仍然要快一些，因为当插入和删除链表节点时链表不需要移动任
何东西，增加效率很多，尤其是复制时间远大于比较时间的时候
- 链表比数组优越的另一个重要方面是链表需要多少内存就用多少内存，并且可以扩大到所有可用内存，数组
的大小在创建时就固定了，所以经常由于数组太大效率低下哦，或者数组太小导致空间溢出。向量是可扩展的
数组，他通过可变长度解决了这个问题但它一般只能进行固定大小的增量扩展，这种情况下在内存使用效率上
还是比链表低
- 有序链表插入和删除某一项最多需要O(N)次比较（平均N/2），因为必须沿着链表上一步一步走才能找到正
确的位置，然而可以在O(1)的时间内找到或删除最小值，因为他总在表头。如果一个应用频繁存取最小值，且
不需要快速插入，那么有序列表是一个有效的方案选择，例如优先级队列可以由有序链表来实现。有序链表可
以用于高效的排序机制


